from typing import Optional

from django.db.models.query import QuerySet

#
# def public_filter(queryset: QuerySet, public):
#     if public is True:
#         return queryset.filter(public=public)
#     else:
#         return queryset
